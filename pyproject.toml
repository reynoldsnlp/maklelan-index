[project]
name = "maklelan-index"
version = "0.1.0"
description = "Scripture reference index of Dan McClellan's YouTube channel"
requires-python = ">=3.12"
dependencies = [
    "innertube>=2.1.0",
    "requests>=2.31.0",
    "youtube-transcript-api>=1.2.4",
]
